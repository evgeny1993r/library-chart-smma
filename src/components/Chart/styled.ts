import { Card, styled } from '@mui/material';

export const Container = styled(Card)({
  height: '100%',
  display: 'grid',
  gridTemplateColumns: '1fr 7.5rem',
  gridTemplateRows: '3rem 1fr 2.5rem',
});
