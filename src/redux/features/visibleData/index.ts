import visibleDataReducer, {
  linkMin,
  linkRange,
  linkStartIdx,
  linkMousePosition,

  setMin,
  setRange,
  setStartIdx,
  setMousePosition,
} from './visibleDataSlice';

export default visibleDataReducer;

export {
  linkMin,
  linkRange,
  linkStartIdx,
  linkMousePosition,

  setMin,
  setRange,
  setStartIdx,
  setMousePosition,
};
